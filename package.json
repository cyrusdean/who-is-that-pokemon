{
  "name": "who-is-that-pokemon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel -p 4000 watch index.html",
    "build": "parcel build index.html --public-url ./",
    "deploy": "cp CNAME /dist && git subtree push --prefix dist origin deploy",
    "prebuild": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.35.2"
  },
  "browserslist": [
    "last 1 Chrome version"
  ]
}
